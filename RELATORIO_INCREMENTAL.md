[2026-09-24 23:54 America/Sao_Paulo]: Planilha de auditoria incluída e colunas clarificadas
Responsável: Codex.
Objetivo: adicionar a auditoria SUMO ao projeto e deixar explícitos os métodos de extração, as descrições dos campos e como editar valores de entrada ou estado de runtime.
Alterações: `SistemaDeSemaforos/auditoria_Sumo_1.27.1.xlsx` (23 abas). A terceira coluna de cada aba de dados agora é “Como extrair”, seguida pelo nome nativo do atributo; “Descrição do dado/parâmetro” esclarece o campo. As novas colunas de edição distinguem “Editável no arquivo local/virgem?” e ferramenta/meio de edição, de “Editável em execução via TraCI?” e seu setter/ferramenta. O resumo explica esses cabeçalhos. Reaproveitados valores, defaults, fontes, janelas e unidades da auditoria anterior.
Justificativa: separar alteração persistente da entrada da alteração temporária na sessão evita que “editável” implique que o XML será gravado por TraCI. Os caminhos XPath, getters, setters, opções e nomes de ferramentas tornam a extração e a edição verificáveis.
Verificação: lido e aberto o `.xlsx` revisado; 23 abas presentes e arquivo ZIP íntegro. Conferidos cabeçalhos e campos nas abas de rede, faixas, defaults, opções, demanda, TraCI, métodos e dados derivados. Conferidos exemplos de chamada TraCI e a tag XML de configuração. Cópia no projeto tem SHA-256 igual ao arquivo revisado. `uberlandia.vehicular.families.16_2_4.net.xml` e `diretrizesIA.txt` mantêm os SHA-256 registrados no início da auditoria. Nenhum teste de simulação foi executado nesta etapa.
Impactos e pendências: a planilha tem cerca de 88 MiB. Nas abas de dados, os nomes de cabeçalho são mais descritivos e as colunas `editavel_local` e `como_alterar` foram substituídas por quatro colunas com significado explícito; scripts consumidores da estrutura anterior devem ser atualizados. O mapa permanece sem alterações.
Continuidade: usar a planilha em `SistemaDeSemaforos/` como cópia vigente da auditoria; manter futuras alterações do mapa e da planilha registradas neste relatório.


[2026-09-25 00:41 America/Sao_Paulo]: Criada versão resumida para leitura humana da auditoria SUMO
Responsável: Codex.
Objetivo: oferecer uma cópia mais curta para análise humana, mantendo intacta a auditoria completa.
Alterações: criado `SistemaDeSemaforos/resumo_humano_Sumo_1.27.1.xlsx`, com quatro abas: visão geral, rede resumida, semáforos e dados/ferramentas. Faixas, cruzamentos, conexões e outros objetos repetitivos foram sintetizados em contagens e perfis agregados; o perfil dos semáforos reúne os 28 programas sem listar todas as fases por ocorrência. A cópia registra que não há demanda nem resultados de execução no projeto e aponta para a planilha integral quando for necessário consultar detalhes.
Justificativa: reduzir o esforço de leitura e manter detalhes por ocorrência na auditoria original, sem descartar ou sobrescrever informação.
Verificação: abertas as duas planilhas com openpyxl; a original manteve 23 abas e o mesmo SHA-256 (`42490f559fcd7e1b953b348f0ae779914edadfaf75aa8068f3f204c7fa259c4e`); a nova contém quatro abas, totaliza 50 linhas de conteúdo incluindo cabeçalhos e passou na verificação de integridade ZIP. Conferida no XML a contagem resumida da rede e dos programas; os 28 programas são `static`, possuem 3, 4 ou 6 fases e ciclo de 90 s. Nenhuma simulação foi executada.
Impactos e pendências: a versão resumida é para orientação e não substitui consultas específicas por `lane`, `junction`, conexão, fase ou atributo na planilha integral. Os totais agregados refletem o mapa atual e devem ser regenerados se ele mudar.
Continuidade: usar `resumo_humano_Sumo_1.27.1.xlsx` para leitura inicial e `auditoria_Sumo_1.27.1.xlsx` como fonte detalhada de auditoria.


[2026-09-25 11:52 America/Sao_Paulo]: Primeiro método independente de geração de demanda random
Responsável: Codex.
Objetivo: gerar pelo terminal aproximadamente 4.800 viagens aleatórias para a rede SUMO existente, separando a criação da demanda de qualquer execução de simulação.
Alterações: criados `SistemaDeSemaforos/demand.py` (função pública `generate_random_demand` e CLI), `Makefile` (alvos `demand-random` e `test`), `DEMANDA.md` (instruções operacionais), `tests/test_demand.py` (oito testes direcionados com `unittest`) e `.gitignore` (demanda gerada e caches Python). Alterado este `RELATORIO_INCREMENTAL.md`, preservando as entradas anteriores. A geração real também criou `SistemaDeSemaforos/demandas/random.trips.xml` e `random.rou.xml`, mantidos localmente e ignorados pelo Git. Rede, planilhas e diretrizes não foram modificadas.
Justificativa: a leitura inicial encontrou somente a rede, planilhas, diretrizes e relatório; não havia `AGENTS.md`, scripts, Makefile nem regras de teste existentes. Foi adotado um único módulo Python, sem dependências Python adicionais, classes, factories ou registries. `make demand-random` chama `python3 -m SistemaDeSemaforos.demand`; opções podem ser encaminhadas por `DEMAND_ARGS`, e `PYTHON` permite selecionar o interpretador. A função recebe rede, diretório, duração e período e retorna o caminho absoluto de `random.rou.xml`, mantendo a futura simulação independente da origem da demanda.
Funcionamento: localiza `randomTrips.py` em `$SUMO_HOME/tools`, sorteia uma seed inteira entre 0 e `2**31 - 1` por chamada e executa a ferramenta com início zero, duração padrão de 7.200 s, período de 1,5 s, classe `passenger` e `--validate`. A opção de saída de rotas aciona `duarouter` apenas para roteamento. Ambos os XMLs são gerados em diretório temporário, conferidos quanto a XML válido, raiz `routes` e presença de viagens/veículos, e então substituem os arquivos anteriores. Falhas de geração ou validação preservam a demanda anterior. A CLI informa quantidade solicitada, quantidade roteada e caminho; erros retornam código diferente de zero. Não há execução de simulação.
Verificação: `make test` aprovado, com oito testes cobrindo chamada do método, parâmetros padrão e personalizados, seed válida sorteada por chamada, comando SUMO, publicação/substituição de arquivos, entradas inválidas e preservação da demanda anterior em falhas do processo ou das saídas. Executado `make demand-random` com SUMO 1.27.1: 4.800 viagens e 4.800 veículos roteados; conferidos os XMLs, IDs únicos, rotas não vazias, seed válida no cabeçalho e partidas de 0 a 7.198,5 s em intervalos de 1,5 s. Executado também o alvo com `--duration 3600 --period 2` e diretório temporário contendo espaços: 1.800 viagens/rotas, partidas de 0 a 3.598 s, conforme os parâmetros. Conferidos `--help`, erros da CLI para ausência de `SUMO_HOME` e período zero (código 1), e ausência de temporários residuais na saída padrão. `git diff --check` sem problemas. Nenhuma simulação ou alteração semafórica foi testada ou executada.
Impactos e pendências: requer Python 3.10+, make, SUMO com `randomTrips.py`, `duarouter` no PATH e `SUMO_HOME` configurado. O diretório padrão é `SistemaDeSemaforos/demandas/`; outra execução substitui seus dois XMLs, e `--output-dir` permite preservar demandas separadas. As 4.800 viagens representam solicitações, não garantem o mesmo total roteado em toda rede/execução nem veículos inseridos ou viagens concluídas. A aleatoriedade está nas origens/destinos; as partidas são regulares. A seed fica no cabeçalho produzido pelo SUMO, mas não há seleção de seeds nem infraestrutura de reprodutibilidade. Testes automatizados simulam as ferramentas externas; as gerações reais descritas acima foram verificações manuais adicionais. Os dois arquivos são publicados sequencialmente, sem transação entre eles ou coordenação de execuções simultâneas no mesmo destino. As observações antigas de ausência de demanda nas planilhas descrevem o estado anterior a esta etapa; as planilhas foram preservadas.
Continuidade: usar `DEMANDA.md` para gerar a demanda e `make test` para verificar o módulo. Próximo passo possível: integração do método de simulação com múltiplas estratégias de demanda, sem precisar conhecer a geração. Foram deliberadamente deixados para versões futuras métodos baseados em dados reais, contexto temporal, horários, dias da semana e medições; RL/PPO, Hazarika, treinamento, calibração, comparação de controladores, seleção de seeds e análises experimentais não fazem parte desta entrega.


[2026-09-25 12:05 America/Sao_Paulo]: Episódios random com demanda nova e interface visual opcional
Responsável: Codex.
Objetivo: guardar os comandos de teste, executar múltiplos episódios com ou sem GUI e manter o código explicável. Esclarecimento do usuário: cada episódio random deve receber uma demanda nova; futuros métodos terão diretrizes próprias. A limpeza foi limitada a arquivos temporários e obsoletos, preservando as planilhas.
Alterações: criados `COMANDOS_TESTE.md`, `SistemaDeSemaforos/simulation.py`, `SistemaDeSemaforos/episodes.py`, `tests/test_simulation.py` e `tests/test_episodes.py`. Atualizados `Makefile` (alvo `run-random` e variável `RUN_ARGS`), `DEMANDA.md` (integração e regra por método), a docstring de `SistemaDeSemaforos/demand.py` e este relatório. Removidos os diretórios de cache `SistemaDeSemaforos/__pycache__/` e `tests/__pycache__/`. As demandas temporárias das verificações foram removidas automaticamente; rede, planilhas, diretrizes e demanda original de 4.800 viagens foram preservadas. Não foram encontrados outros arquivos obsoletos que justificassem exclusão.
Justificativa: a geração continua em `demand.py`; `run_simulation` em `simulation.py` recebe arquivos prontos e inicia uma única simulação, sem conhecer o método de demanda. `run_random_episodes` em `episodes.py` organiza a regra específica de random: gerar, executar, aguardar e repetir. Cada episódio utiliza um processo novo, sem reaproveitar estado anterior. Docstrings explicam propósito, entradas, efeitos e relações entre módulos, sem classes ou infraestrutura genérica de estratégias.
Funcionamento: `make run-random RUN_ARGS='--episodes 3'` executa três episódios sem interface; acrescentar `--gui` usa `sumo-gui`, com início e fechamento automáticos para liberar o episódio seguinte. Cada episódio chama novamente o gerador (e, portanto, sorteia uma seed) e salva sua demanda em `SistemaDeSemaforos/demandas/episodios/episodio_NNN/` por padrão. Os parâmetros `--duration` e `--period` configuram a demanda, mantendo os padrões 7.200 s e 1,5 s. `--end` limita o tempo simulado somente quando solicitado; sem ele, o SUMO esgota a demanda. Rede e diretório são configuráveis. Falhas interrompem o lote; a CLI trata Ctrl+C com código 130. `make demand-random` permanece responsável apenas por geração.
Verificação: `make test` aprovado com 21 testes (oito de demanda, seis de simulação e sete de episódios); a execução final usou `PYTHONDONTWRITEBYTECODE=1 make test` para não recriar os caches removidos. Os novos testes verificam geração nova antes de cada simulação, ordem sequencial, diretórios separados, parâmetros, seleção de binário, flags GUI, limites, erros e interrupção do lote em falhas. Executados dois episódios reais sem GUI, com `--duration 30 --period 5`, até encerramento natural: seis viagens/rotas por episódio, partidas de 0 a 25 s e seeds válidas diferentes observadas nos cabeçalhos. Executados também dois episódios reais com `sumo-gui`, mesmos parâmetros de demanda e `--end 60`: processos e lote encerraram com código 0, com início/fechamento automáticos e seis veículos roteados em cada diretório. Os quatro episódios usaram diretórios temporários com espaços nos caminhos. Conferidos `--help`, rejeição de `--episodes 0` sem geração/simulação e `git diff --check`. As verificações reais desta etapa usaram demandas pequenas; não foi executado um lote completo de simulações com 4.800 veículos por episódio.
Impactos e pendências: a simulação agora depende de `sumo` no PATH, ou `sumo-gui` e uma sessão gráfica quando `--gui` é utilizado; testes automatizados continuam sem essas dependências. Os arquivos por episódio são demandas, não resultados estatísticos. Uma nova execução no mesmo destino substitui as demandas dos episódios correspondentes; diretórios antigos com outros números permanecem e não significam episódios executados no lote atual. Usar outro `--output-dir` preserva uma execução. `--end` pode deixar viagens em andamento ou ainda não iniciadas, e não representa conclusão de todas as viagens. Caches Python são regeneráveis e podem reaparecer em comandos comuns. Nenhuma alteração de lógica semafórica ou de controle foi introduzida.
Continuidade: consultar `COMANDOS_TESTE.md` para testes automatizados, execuções rápidas e episódios com/sem GUI. Novas estratégias devem definir suas próprias regras de geração e execução; não aplicar automaticamente a renovação por episódio de random a dados medidos/contextuais. Reutilizar `run_simulation` como consumidor de arquivos, mantendo a política de episódios fora da execução individual. RL, calibração, comparações experimentais e implementação de outras estratégias continuam fora do escopo.


[2026-09-25 12:23 America/Sao_Paulo]: Demanda random simplificada e documentada
Responsável: Codex.
Objetivo: tornar a geração de demanda random fácil de entender, sem adicionar funcionalidade e preservando os comandos e comportamentos existentes.
Alterações: reescritos `DEMANDA.md` e `COMANDOS_TESTE.md` em uma ordem operacional, com parágrafos curtos, fluxo visual, exemplos e tabelas pequenas. Simplificados `SistemaDeSemaforos/demand.py`, `SistemaDeSemaforos/simulation.py`, `Makefile` e `.gitignore`. Atualizados `tests/test_demand.py` e `tests/test_simulation.py`. Removidos `SistemaDeSemaforos/episodes.py` e `tests/test_episodes.py`; a coordenação de episódios e seus testes foram incorporados aos arquivos de simulação. O cache Python criado durante a verificação real também foi removido. Nenhum novo arquivo foi criado nesta refatoração.
Justificativa: os três arquivos Python foram reduzidos a dois sem perder a separação necessária. `demand.py` tem uma frase de responsabilidade: escolher a seed, gerar as viagens e calcular suas rotas. `simulation.py` tem outra: executar o SUMO e coordenar os episódios random. A função `run_simulation` continua recebendo uma demanda pronta, o que permite que outro método futuro reutilize a execução sem conhecer o gerador random. Constantes nomeadas substituíram valores repetidos, a seed agora usa diretamente `random.randint`, as docstrings declaram entrada, saída e uso normal, e os comandos externos foram formatados para leitura por pares de opção e valor.
Decisões sobre os XMLs: `random.trips.xml` foi mantido porque é o resultado da amostragem de origens, destinos e partidas e é a entrada usada pelo `duarouter`; ele também permite inspecionar o que foi sorteado. `random.rou.xml` foi mantido porque contém as rotas calculadas e é o único dos dois carregado pela simulação SUMO. O fluxo confirmado na implementação instalada é `randomTrips.py` → `random.trips.xml` → `duarouter` → `random.rou.xml` → SUMO. Os dois arquivos continuam em `SistemaDeSemaforos/demandas/`, ignorados pelo Git, pois são saídas automáticas, variáveis e recriáveis, e não cenários permanentes. A documentação orienta versionar separadamente uma demanda que futuramente seja declarada cenário fixo.
Funcionamento: o comando final de geração continua sendo `make demand-random`. Ele usa por padrão 7.200 segundos e período de 1,5 segundo, solicitando `7200 / 1.5 = 4800` viagens, escolhe uma seed aleatória, gera os dois XMLs e informa o caminho de `random.rou.xml`. `make run-random` continua gerando uma demanda nova antes de cada episódio e pode usar `--gui`. O Makefile recebeu somente comentários diretos sobre a responsabilidade e as saídas de cada alvo; suas interfaces foram preservadas, exceto pelo caminho interno do módulo removido, agora apontando para `SistemaDeSemaforos.simulation`.
Verificação: executado `make test`; 16 testes passaram em 0,026 s. Os oito testes de demanda e os oito testes consolidados de simulação cobrem seed, comando `randomTrips.py`, dois XMLs, preservação após falhas, execução headless/GUI, entradas inválidas, nova demanda por episódio e interrupção do lote. Executados também `python3 -m SistemaDeSemaforos.demand --help`, `python3 -m SistemaDeSemaforos.simulation --help` e `git diff --check`, todos com sucesso. Em verificação real temporária, `make demand-random` gerou seis viagens e seis rotas com `--duration 30 --period 5`; depois `make run-random` gerou outra demanda e concluiu um episódio SUMO sem GUI com os mesmos parâmetros e `--end 60`. Os temporários foram removidos automaticamente.
Impactos e pendências: consumidores que chamavam diretamente `python3 -m SistemaDeSemaforos.episodes` devem usar `python3 -m SistemaDeSemaforos.simulation`; os comandos públicos do Makefile não mudaram. O total de 4.800 representa viagens solicitadas, e a validação de conectividade pode produzir menos veículos roteados. Não foram adicionados controle de seeds, RL, treinamento, reprodutibilidade experimental ou novos métodos de demanda.
Continuidade: começar por `DEMANDA.md` para entender o fluxo e por `COMANDOS_TESTE.md` para executar os comandos. Ao adicionar outro método de demanda, produzir um arquivo de rotas e passá-lo a `run_simulation`, definindo separadamente a política de episódios desse método.
